# Dev environment overrides — applies to all clusters in dev, all regions.
# Inherits from: config/base/values.yaml

logLevel: debug

autoscaling:
  enabled: false

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
