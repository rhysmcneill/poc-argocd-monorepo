# Cluster metadata — informational reference for this cluster.
# The ApplicationSet no longer reads this file directly; deployment intent
# is expressed via app-config.yaml files in each app subdirectory.

env: dev
region: eu-west-1
cluster: eks-local-dev
server: https://kubernetes.default.svc
